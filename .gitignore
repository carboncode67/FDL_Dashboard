# Secrets — never commit
.env
.env.local
.env*.local
.env.lab
.mcp.json

# Local dev compose file — has a real DB password inline once configured.
# Copy sample_compose.yaml to docker-compose.yml and edit that instead.
docker-compose.yml

# Working planning doc — not project documentation, may contain notes/creds.
Planned Changes.md

# Dependencies and build output
node_modules/
.next/
out/
dist/
build/

# Python
.venv/
__pycache__/
*.pyc

# Uploaded files (PII) — stored on disk, not in the repo
upload-data/

# macOS / IDE
.DS_Store
.idea/

# Logs
*.log
npm-debug.log*

# Syncthing conflict duplicates
*sync-conflict*
