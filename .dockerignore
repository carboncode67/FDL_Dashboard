node_modules
.next
.git
.env*
upload-data
.DS_Store
.claude
*.md
# Syncthing conflict duplicates — must never enter the build context (they
# carry stale code and break the typecheck). Matches any extension.
*sync-conflict*
